#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node" --harmony "$basedir/../pm2-windows-service/bin/pm2-service-install" "$@"
else 
  exec node --harmony "$basedir/../pm2-windows-service/bin/pm2-service-install" "$@"
fi
